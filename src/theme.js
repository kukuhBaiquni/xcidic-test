import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#244B9C',
      light: '#007cbd',
    },
    secondary: {
      main: '#FF7C00',
    },
    textGeneral: {
      main: '#595959',
    },
  },
})

export default theme
